arr = []
n = int(input("Enter number of elements: "))

for i in range(n):
    m = int(input("Enter value: "))
    arr.append(m)

# Find maximum
max_val = arr[0]

for i in range(1, n):
    if arr[i] > max_val:
        max_val = arr[i]

print("Array is:", arr)
print("Max is:", max_val)
